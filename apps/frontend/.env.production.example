# Configuration pour l'environnement de production
# Ce fichier est automatiquement chargé par Vite en mode production

# URL de l'API backend en production (à adapter selon votre domaine)
# Si non défini, utilise automatiquement l'URL actuelle du site
VITE_API_URL=http://yourdomain:port

# URL pour les connexions Socket.IO en production (à adapter selon votre domaine)
# Si non défini, utilise automatiquement l'URL actuelle du site
VITE_SOCKET_URL=http://yourdomain:port

# Mode de production
VITE_DEBUG=false

# Niveau de log en production
VITE_LOG_LEVEL=error

# Optimisations de production
VITE_OPTIMIZE=true